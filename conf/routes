# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Return all primes between start and end using specific sieve type
GET 	/sieve						controllers.SieveController.generate(type: String, start: Int, end: Int)
# Return all queries done and their results
GET     /queries 							controllers.SieveController.getQueries()
